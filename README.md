# Obyw.one

Design | Digital Agency — landing page.

## Setup

Open `index.html` in a browser. No build step required.

## Structure

```
index.html        — single-page site
css/style.css     — all styles (dark theme, responsive)
js/main.js        — splash transition, project tabs, scroll
assets/           — placeholder for logo and images
```
